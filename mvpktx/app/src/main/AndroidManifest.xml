<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools" package="com.kiwilss.mvpktx">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".MyApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme" tools:ignore="GoogleAppIndexingWarning"
            tools:replace="android:name">
        <activity android:name=".MainActivity"
                  android:launchMode="singleTask"
                  >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".test.login.TestLoginActivity" android:label="@string/testLogin"
                  android:screenOrientation="portrait"/>

        <activity android:name=".test.shop_detail.ShopDetailActivity" android:label="详情测试"
                  android:screenOrientation="portrait"/>

        <activity android:name=".test.register.RegisterActivity" android:label="测试注册"
                  android:screenOrientation="portrait"/>

        <!--im相关-->
        <activity android:name=".im.login.IMLoginActivity" android:label="登录"
                  android:screenOrientation="portrait"/>

        <activity android:name=".im.home.IMHomeActivity" android:label="首页"
                  android:screenOrientation="portrait"/>

    </application>

</manifest>